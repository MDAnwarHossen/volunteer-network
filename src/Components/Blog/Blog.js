import React from 'react';
import Header from '../Header/Header';

const Blog = () => {
    return (
        <div className="container-fluid">
            <Header></Header>
            <h1>There is no blog right now</h1>
        </div>
    );
};

export default Blog;